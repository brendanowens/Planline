import React from 'react';

export class CalendarAdd extends React.Component {
    render() {
        return (
            <span>Add a new calendar entry.</span>
        );
    }
}

export default (CalendarAdd);
